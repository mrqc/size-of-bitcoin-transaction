# size-of-bitcoin-transaction
The script ```size-of-transaction.py``` calculates the size of the bitcoin transaction, for correct calculation modify the values inside the script. Used it with python 2.x.
